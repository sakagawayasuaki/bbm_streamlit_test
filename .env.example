# Google Cloud Speech Services設定（STT/TTS統合）
GOOGLE_CLOUD_PROJECT_ID=your_google_cloud_project_id_here
# Google Cloud認証は以下のいずれかの方法で設定:
# 1. GOOGLE_APPLICATION_CREDENTIALS環境変数でサービスアカウントJSONファイルを指定
# 2. gcloud auth application-default login コマンドを実行
# 3. Google Cloud Platform上で実行（自動認証）

# 例:
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/service-account-key.json

# 音声設定
AUDIO_SAMPLE_RATE=16000
AUDIO_CHANNELS=1